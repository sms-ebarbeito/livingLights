
void initConfigTaskScheduler() {
  /**
   * Leer configuracion y atachar tarea
   
   int s = atoi(server_seconds);
   if (s == 0 ) {
      Serial.println("Tarea 'sendToServer' desactivada");
   } else {
      //taskScheduler.attach(s, sendToServer);
      //Serial.print("Tarea 'sendToServer' configurada a ");
      //Serial.print(s);
      //Serial.println(" segundos"); 
   }
   **/
   
}
